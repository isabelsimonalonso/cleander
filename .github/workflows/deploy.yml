# Publica la aplicación completa en GitHub Pages.
#
# ⚠️ PUBLICACIÓN AUTOMÁTICA DESACTIVADA a propósito.
#    Solo se lanza a mano desde la pestaña Actions de GitHub («Run workflow»),
#    así que hacer push NO republica nada.
#
#    Es el botón de abrir, y también el de volver: lánzalo para publicar la
#    web y lánzalo otra vez para recuperarla después de un mantenimiento.
#    Para cerrarla un rato, usa «Publicar · página de mantenimiento».
#
#    Para publicar en cada push, descomenta las tres líneas de `push:`.
#
# La configuración de Supabase se lee de frontend/.env.production, que está
# en el repositorio a propósito (ver la explicación dentro de ese archivo).
name: Publicar · la aplicación completa

on:
  # push:
  #   branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  construir:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Instalar dependencias
        working-directory: frontend
        run: npm ci

      - name: Pasar las pruebas
        working-directory: frontend
        run: npm test

      - name: Construir
        working-directory: frontend
        run: npm run build

      - name: Comprobar que la web lleva la configuración de Supabase
        working-directory: frontend
        run: |
          if ! grep -rq "supabase.co" dist/assets/*.js; then
            echo "::error::La web se ha construido sin la URL de Supabase."
            echo "Revisa frontend/.env.production"
            exit 1
          fi
          echo "Configuración de Supabase incluida en la web."

      - uses: actions/configure-pages@v5

      - uses: actions/upload-pages-artifact@v3
        with:
          path: frontend/dist

  desplegar:
    needs: construir
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.despliegue.outputs.page_url }}
    steps:
      - id: despliegue
        uses: actions/deploy-pages@v4
